From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: robert-brunson-fragile <siinspam@gmail.com>
Date: Wed, 1 Jul 2026 14:10:43 -0700
Subject: [PATCH 20/21] feat(api): realtime resumable session sync engine

Upgrade /api/sessions/{id}/subscribe from a 0.5s polling loop with
split, client-managed cursors into a push-based, standards-resumable
sync stream:

- SessionSyncNotifier: per-session wakeup fanout. Every in-process
  event writer signals subscribers when it commits, so delivery no
  longer waits on a poll timer. A 2s poll fallback remains for writers
  in other processes sharing state.db.
- hermes_state module-level session-event listener hook: the agent
  persists transcripts through its own SessionDB instance, so the
  notifier registers process-wide and wakes subscribers on any
  instance's session_events write (fired post-commit, never inside the
  write lock).
- Composite resume cursor v1:<event_cursor>:<run_id>:<run_seq> carried
  as the SSE id: on every frame. Reconnects resume via the standard
  Last-Event-ID header (EventSource works with zero client
  bookkeeping) or an explicit ?resume= param; the legacy
  after/cursor/run_after/after_seq params keep working. Header beats
  query params because EventSource re-sends the original stale query
  string on auto-reconnect.
- Startup recovery now appends a synthetic run.interrupted replay
  event (at last_seq + 1) for runs orphaned by a server restart, so
  resuming clients learn the run died instead of hanging; fresh
  snapshots show status=interrupted. run.status events surface
  status transitions that have no replayable event (e.g. /halt).
- ResponseStore writes serialized behind a lock (agent callbacks write
  from executor threads onto a check_same_thread=False connection).
- docs/api-session-sync.md documents the full protocol; capabilities
  advertise session_sync_resume.

Tests: tests/gateway/test_session_sync.py covers composite cursors,
Last-Event-ID reconnects, legacy params, multi-client fanout,
push-beats-poll latency, cross-thread/instance wakeups, restart
interruption visibility, and join-in-progress/run.switch;
TestSessionEventListeners covers the hermes_state hook.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

diff --git a/docs/api-session-sync.md b/docs/api-session-sync.md
new file mode 100644
index 000000000..cb707e3ad
--- /dev/null
+++ b/docs/api-session-sync.md
@@ -0,0 +1,270 @@
+# API session sync
+
+This document describes the Hermes API-server session sync engine: how a
+desktop/mobile/web client attaches to a session over HTTP, receives history and
+live events, and resumes after any disconnect — including an API-server
+restart — without losing or duplicating events.
+
+## Goals
+
+- Re-open a session after app close/tab switch/device switch.
+- Receive current persisted history immediately.
+- Join a session while a run is already streaming and catch up on missed
+  deltas/tool events.
+- Keep more than one client attached without stealing events from the
+  initiating client.
+- Resume from a single opaque cursor via the SSE standard `Last-Event-ID`
+  mechanism — a browser `EventSource` reconnects correctly with zero client
+  bookkeeping.
+- Realtime delivery: events are pushed to subscribers when they are written,
+  not discovered on a polling timer.
+- Learn that an in-flight run died when the server restarted mid-turn.
+
+## Primary endpoint: session subscription SSE
+
+```http
+GET /api/sessions/{session_id}/subscribe
+Authorization: Bearer <API_SERVER_KEY>
+```
+
+For browser `EventSource`, where custom headers are awkward, query-token auth
+is also accepted:
+
+```http
+GET /api/sessions/{session_id}/subscribe?access_token=<API_SERVER_KEY>
+```
+
+The response is `text/event-stream`. The first event is always
+`session.snapshot`.
+
+Snapshot shape:
+
+```json
+{
+  "object": "hermes.session.snapshot",
+  "session_id": "api_...",
+  "session": { "id": "api_...", "title": "...", "message_count": 2, "...": "..." },
+  "messages": [
+    { "id": 1, "role": "user", "content": "...", "items": [...] },
+    { "id": 2, "role": "assistant", "content": "...", "items": [...] }
+  ],
+  "items": [
+    { "id": "msg:1", "type": "message", "role": "user", "content": "..." }
+  ],
+  "latest_event_cursor": "123",
+  "active_run": {
+    "object": "hermes.session.run",
+    "run_id": "run_...",
+    "session_id": "api_...",
+    "status": "running",
+    "last_seq": 4,
+    "draft": "partial assistant text",
+    "reasoning": "partial reasoning/tool progress text",
+    "tool_status": "terminal…"
+  },
+  "cursor": "v1:123:run_...:4"
+}
+```
+
+After the snapshot, the stream emits:
+
+1. **Durable session events** from `state.db` (`session.item.upserted`,
+   `session.ended`, `session.reopened`, `session.transcript.replaced`,
+   `response.completed`, …). These carry a global `cursor` field.
+2. **Replayable active-run events** from `response_store.db` (`run.started`,
+   `message.started`, `assistant.delta`, `tool.progress`, `tool.started`,
+   `tool.completed`, `assistant.completed`, `run.completed`, `done`,
+   `error`, `run.interrupted`). These carry a per-run integer `seq` field.
+3. **`run.switch`** when the session's latest run changes while the
+   subscription is open. The payload contains the new run snapshot; the
+   server resets the per-run replay to `seq 0` for the new run automatically.
+4. **`run.status`** when the active run's status changes without a
+   corresponding replayable event (for example a `/v1/sessions/{id}/halt`).
+   The payload contains the updated run snapshot.
+
+Example run event:
+
+```text
+id: v1:123:run_abc:3
+event: assistant.delta
+data: {"run_id":"run_abc","session_id":"api_...","seq":3,"message_id":"msg_...","delta":"hello"}
+```
+
+## Resume cursor
+
+Every SSE frame (including the snapshot) carries an `id:` field containing a
+composite resume token:
+
+```text
+v1:<session_event_cursor>:<run_id>:<run_seq>
+```
+
+- `session_event_cursor` — global durable-event cursor ("everything up to and
+  including this event has been delivered").
+- `run_id` / `run_seq` — the active run at that point and the last delivered
+  per-run sequence number. The run slot is empty when no run has been observed.
+
+The token is opaque to clients: store the most recent `id` (or let
+`EventSource` do it for you) and present it on reconnect. Tokens are NOT
+comparable across sessions.
+
+### Reconnecting
+
+Three ways to resume, in server-side precedence order:
+
+1. **`Last-Event-ID` header** — sent automatically by `EventSource` on
+   reconnect. Highest precedence: an auto-reconnect re-sends the original
+   (stale) query string, so the header must win over query params.
+2. **`?resume=<token>`** — explicit composite-token query param for clients
+   that manage reconnects themselves. Malformed tokens are a 400.
+3. **Legacy split params** — `?after=<session_event_cursor>` (alias
+   `cursor`) and `?run_after=<run_seq>` (alias `after_seq`), unchanged from
+   the original protocol.
+
+Semantics on resume:
+
+- Durable session events replay strictly after `session_event_cursor`.
+- If the token's `run_id` still matches the session's latest run, run events
+  replay strictly after `run_seq`.
+- If the session has moved on to a different run, the stored `run_seq` is
+  ignored and the new run replays from `seq 0` (a `run.switch` event tells the
+  client to reset).
+
+With no cursor at all, durable events start from the snapshot-time cursor (the
+snapshot already contains the persisted history, so nothing is duplicated) and
+the active run replays from `seq 0` (so a mid-run joiner sees the full
+in-progress turn).
+
+### Server-restart interruption
+
+If the API server restarts while a run is in flight, startup recovery marks
+the run `interrupted` and appends a synthetic `run.interrupted` event at
+`last_seq + 1` in the run's replay log. Consequences:
+
+- A client resuming from its pre-restart cursor replays `run.interrupted`
+  instead of waiting forever for more deltas.
+- A fresh subscriber sees `active_run.status == "interrupted"` in the
+  snapshot.
+
+## Realtime characteristics
+
+- **Push:** every event writer in the API-server process signals subscribers
+  when it commits (via an in-process per-session notifier; transcript writes
+  from the agent's own `SessionDB` instance are covered by a module-level
+  listener hook in `hermes_state`). Delivery latency is not bound to any
+  polling interval.
+- **Poll fallback (2s):** writes from *other processes* sharing the same
+  `state.db` (CLI, gateway) cannot signal this process; subscribers re-check
+  the durable stores every 2 seconds to pick those up.
+- **Keepalive:** after 30 seconds of silence the stream emits a
+  `: keepalive` comment so intermediaries don't drop the connection.
+
+## Starting work in a session
+
+Use the persisted session stream endpoint to start a turn:
+
+```http
+POST /api/sessions/{session_id}/chat/stream
+Content-Type: application/json
+Authorization: Bearer <API_SERVER_KEY>
+
+{"message":"do the task"}
+```
+
+The initiating client may consume that response directly, but it does not
+have to be the only consumer — the run's events are durably persisted and any
+other client receives them via `/subscribe`. The agent turn keeps running if
+the initiating client disconnects; the initiator can itself rejoin through
+`/subscribe` with its last cursor.
+
+Queue a follow-up turn while a run is active with
+`POST /v1/sessions/{session_id}/queue`.
+
+For non-initiating clients:
+
+1. Connect to `/api/sessions/{session_id}/subscribe`.
+2. Render `session.snapshot.messages` immediately.
+3. If `session.snapshot.active_run.status` is `queued` or `running`, keep
+   rendering incoming `assistant.delta`/tool/run events.
+
+## Companion endpoints
+
+- `GET /api/sessions` — list sessions; each row includes lightweight live
+  state (`active`, `status`, `sync_state`, `active_run`).
+- `POST /api/sessions` — create an empty session.
+- `GET /api/sessions/{session_id}` — session metadata with live-state fields.
+- `GET /api/sessions/{session_id}/messages` — persisted message history only.
+- `GET /v1/sessions/{session_id}/items` — stable item projection.
+- `GET /v1/sessions/{session_id}/events?after=<cursor>` — durable
+  session-event replay only (single poll, not streaming).
+- `GET /v1/runs/{run_id}` — run status polling, including
+  `waiting_for_approval` with the pending approval request payload; respond
+  via `POST /v1/runs/{run_id}/approval`. (The former
+  `GET /v1/runs/{run_id}/events` single-consumer SSE stream was removed; run
+  events are observed durably via `/subscribe`.)
+
+## Client rendering notes
+
+- Treat `messages` in `session.snapshot` as the authoritative persisted
+  transcript at attach time.
+- Treat `items` as the normalized render model for
+  message/tool-call/tool-result display.
+- Accumulate `assistant.delta` events by `message_id` for the current
+  in-progress assistant draft.
+- On `assistant.completed`, replace the draft with the completed content.
+- On `run.completed`, reconcile with `messages` in that payload if present;
+  Hermes includes the interleaved assistant/tool transcript for the completed
+  turn when available.
+- On `run.interrupted` or a `run.status` transition to a terminal state,
+  stop rendering the run as live.
+- Tool events may arrive as both renderable run events (`tool.progress`,
+  `tool.started`, `tool.completed`) and durable item upserts
+  (`session.item.upserted`) depending on the path. Use stable item ids and
+  tool_call ids to de-dupe.
+
+## Capability discovery
+
+`GET /v1/capabilities` advertises:
+
+- `features.session_subscribe_sse: true`
+- `features.session_sync_resume: true` — composite `id:` cursors +
+  `Last-Event-ID`/`?resume=` reconnects.
+- `endpoints.session_subscribe`
+
+## Implementation pointers
+
+- API-server adapter: `gateway/platforms/api_server.py`
+  - `SessionSyncNotifier` — per-session push wakeup fanout.
+  - `_encode_sync_cursor` / `_parse_sync_cursor` — composite token codec.
+  - `_handle_session_subscribe` — snapshot + resumable live SSE.
+  - `_handle_session_chat_stream` — persists replayable per-run events into
+    `ResponseStore.session_run_events`.
+  - `_record_live_tool_session_item` — publishes provisional tool call/result
+    item upserts into `SessionDB.session_events`.
+  - `ResponseStore.mark_incomplete_session_runs_interrupted` — startup
+    recovery + synthetic `run.interrupted` replay events.
+- Durable stores:
+  - `hermes_state.py` / `SessionDB.session_events` for session items and
+    global cursor-based sync; `register_session_event_listener` is the
+    module-level write hook the notifier attaches to.
+  - `ResponseStore.session_runs` / `session_run_events` in
+    `response_store.db` for per-run assistant deltas and lifecycle replay.
+
+## Verified behavior
+
+Regression coverage:
+
+- `tests/gateway/test_session_sync.py` — composite cursors, `Last-Event-ID`
+  reconnects, legacy params, multi-client fanout, push-beats-poll latency,
+  cross-thread/instance wakeups, restart interruption visibility,
+  join-in-progress and `run.switch`.
+- `tests/gateway/test_session_api.py` — snapshot/replay basics, chat-stream
+  run persistence, restart recovery of the run store.
+- `tests/test_hermes_state.py::TestSessionEventListeners` — the module-level
+  write hook.
+
+```bash
+uv run --extra dev --extra messaging python -m pytest \
+  tests/gateway/test_session_sync.py tests/gateway/test_session_api.py \
+  tests/test_hermes_state.py -q -o addopts=
+```
diff --git a/gateway/platforms/api_server.py b/gateway/platforms/api_server.py
index 4a5b4395a..084c7d4d3 100644
--- a/gateway/platforms/api_server.py
+++ b/gateway/platforms/api_server.py
@@ -43,6 +43,7 @@ import re
 import sqlite3
 import subprocess
 import tempfile
+import threading
 import time
 import uuid
 from pathlib import Path
@@ -529,6 +530,10 @@ class ResponseStore:
 
     def __init__(self, max_size: int = MAX_STORED_RESPONSES, db_path: str = None):
         self._max_size = max_size
+        # Run-event writers include agent callbacks on executor threads and
+        # handlers on the event loop; one connection with
+        # check_same_thread=False needs explicit serialization.
+        self._write_lock = threading.Lock()
         if db_path is None:
             try:
                 from hermes_cli.config import get_hermes_home
@@ -705,75 +710,106 @@ class ResponseStore:
             encoded_user_message = json.dumps(user_message, default=str)
         except (TypeError, ValueError):
             encoded_user_message = json.dumps(str(user_message))
-        self._conn.execute(
-            """INSERT OR REPLACE INTO session_runs
-               (run_id, session_id, status, user_message, error, created_at, updated_at, last_seq)
-               VALUES (?, ?, 'queued', ?, NULL, ?, ?, 0)""",
-            (run_id, session_id, encoded_user_message, now, now),
-        )
-        self._conn.commit()
+        with self._write_lock:
+            self._conn.execute(
+                """INSERT OR REPLACE INTO session_runs
+                   (run_id, session_id, status, user_message, error, created_at, updated_at, last_seq)
+                   VALUES (?, ?, 'queued', ?, NULL, ?, ?, 0)""",
+                (run_id, session_id, encoded_user_message, now, now),
+            )
+            self._conn.commit()
 
     def put_session_run_event(self, run_id: str, event_name: str, payload: Dict[str, Any]) -> Dict[str, Any]:
         """Append a replayable event and update the run snapshot."""
         now = time.time()
-        row = self._conn.execute(
-            "SELECT session_id, last_seq FROM session_runs WHERE run_id = ?",
-            (run_id,),
-        ).fetchone()
-        if row is None:
-            return payload
-
-        seq = int(row[1] or 0) + 1
-        stored_payload = dict(payload)
-        stored_payload.setdefault("run_id", run_id)
-        stored_payload.setdefault("session_id", row[0])
-        stored_payload["seq"] = seq
-        stored_payload.setdefault("ts", now)
-
-        status = None
-        error = None
-        if event_name == "run.started":
-            status = "running"
-        elif event_name == "run.completed":
-            status = "completed"
-        elif event_name == "error":
-            status = "failed"
-            error = str(stored_payload.get("message") or "stream failed")
+        with self._write_lock:
+            row = self._conn.execute(
+                "SELECT session_id, last_seq FROM session_runs WHERE run_id = ?",
+                (run_id,),
+            ).fetchone()
+            if row is None:
+                return payload
+
+            seq = int(row[1] or 0) + 1
+            stored_payload = dict(payload)
+            stored_payload.setdefault("run_id", run_id)
+            stored_payload.setdefault("session_id", row[0])
+            stored_payload["seq"] = seq
+            stored_payload.setdefault("ts", now)
+
+            status = None
+            error = None
+            if event_name == "run.started":
+                status = "running"
+            elif event_name == "run.completed":
+                status = "completed"
+            elif event_name in ("error", "run.failed"):
+                status = "failed"
+                error = str(stored_payload.get("message") or "stream failed")
+            elif event_name in ("run.cancelled", "run.interrupted"):
+                status = "interrupted"
 
-        self._conn.execute(
-            """INSERT OR REPLACE INTO session_run_events
-               (run_id, seq, event_name, payload, created_at)
-               VALUES (?, ?, ?, ?, ?)""",
-            (run_id, seq, event_name, json.dumps(stored_payload, default=str), now),
-        )
-        if status is None:
-            self._conn.execute(
-                "UPDATE session_runs SET updated_at = ?, last_seq = ? WHERE run_id = ?",
-                (now, seq, run_id),
-            )
-        else:
             self._conn.execute(
-                """UPDATE session_runs
-                   SET status = ?, error = COALESCE(?, error), updated_at = ?, last_seq = ?
-                   WHERE run_id = ?""",
-                (status, error, now, seq, run_id),
+                """INSERT OR REPLACE INTO session_run_events
+                   (run_id, seq, event_name, payload, created_at)
+                   VALUES (?, ?, ?, ?, ?)""",
+                (run_id, seq, event_name, json.dumps(stored_payload, default=str), now),
             )
-        self._conn.commit()
+            if status is None:
+                self._conn.execute(
+                    "UPDATE session_runs SET updated_at = ?, last_seq = ? WHERE run_id = ?",
+                    (now, seq, run_id),
+                )
+            else:
+                self._conn.execute(
+                    """UPDATE session_runs
+                       SET status = ?, error = COALESCE(?, error), updated_at = ?, last_seq = ?
+                       WHERE run_id = ?""",
+                    (status, error, now, seq, run_id),
+                )
+            self._conn.commit()
         return stored_payload
 
     def mark_incomplete_session_runs_interrupted(self) -> int:
-        """Mark runs left in-flight by a previous API-server process as interrupted."""
+        """Mark runs left in-flight by a previous API-server process as interrupted.
+
+        Also appends a synthetic ``run.interrupted`` event to each affected
+        run's replay log so subscribers resuming from a pre-restart cursor
+        learn the run died instead of waiting forever for more deltas.
+        """
+        interrupted_message = "Hermes API server restarted while this run was active."
         now = time.time()
-        cur = self._conn.execute(
-            """UPDATE session_runs
-               SET status = 'interrupted',
-                   error = COALESCE(error, 'Hermes API server restarted while this run was active.'),
-                   updated_at = ?
-               WHERE status IN ('queued', 'running')""",
-            (now,),
-        )
-        self._conn.commit()
-        return int(cur.rowcount or 0)
+        with self._write_lock:
+            rows = self._conn.execute(
+                """SELECT run_id, session_id, last_seq FROM session_runs
+                   WHERE status IN ('queued', 'running')""",
+            ).fetchall()
+            for run_id, session_id, last_seq in rows:
+                seq = int(last_seq or 0) + 1
+                payload = {
+                    "run_id": run_id,
+                    "session_id": session_id,
+                    "seq": seq,
+                    "status": "interrupted",
+                    "message": interrupted_message,
+                    "ts": now,
+                }
+                self._conn.execute(
+                    """INSERT OR REPLACE INTO session_run_events
+                       (run_id, seq, event_name, payload, created_at)
+                       VALUES (?, ?, 'run.interrupted', ?, ?)""",
+                    (run_id, seq, json.dumps(payload, default=str), now),
+                )
+                self._conn.execute(
+                    """UPDATE session_runs
+                       SET status = 'interrupted',
+                           error = COALESCE(error, ?),
+                           updated_at = ?, last_seq = ?
+                       WHERE run_id = ?""",
+                    (interrupted_message, now, seq, run_id),
+                )
+            self._conn.commit()
+        return len(rows)
 
     def get_latest_session_run(self, session_id: str) -> Optional[Dict[str, Any]]:
         row = self._conn.execute(
@@ -891,6 +927,100 @@ else:
     cors_middleware = None  # type: ignore[assignment]
 
 
+class SessionSyncNotifier:
+    """Per-session wakeup fanout for /subscribe streams.
+
+    Each subscriber registers its own asyncio.Event so concurrent subscribers
+    never race on a shared flag. ``notify()`` is safe to call from executor
+    threads (agent callbacks) — it marshals onto the bound event loop. When no
+    loop is bound (unit tests calling handlers directly, pre-startup writes)
+    it silently no-ops and subscribers fall back to their poll interval.
+    """
+
+    def __init__(self) -> None:
+        self._loop: Optional[asyncio.AbstractEventLoop] = None
+        self._waiters: Dict[str, set] = {}
+
+    def bind_loop(self, loop: "asyncio.AbstractEventLoop") -> None:
+        self._loop = loop
+
+    def attach(self, session_id: str) -> "asyncio.Event":
+        if self._loop is None:
+            # Subscribers only exist on a running loop; self-bind so streams
+            # served without an explicit bind_loop() (tests, embedded apps)
+            # still get push wakeups.
+            try:
+                self._loop = asyncio.get_running_loop()
+            except RuntimeError:
+                pass
+        event = asyncio.Event()
+        self._waiters.setdefault(session_id, set()).add(event)
+        return event
+
+    def detach(self, session_id: str, event: "asyncio.Event") -> None:
+        waiters = self._waiters.get(session_id)
+        if not waiters:
+            return
+        waiters.discard(event)
+        if not waiters:
+            self._waiters.pop(session_id, None)
+
+    def notify(self, session_id: str) -> None:
+        loop = self._loop
+        if loop is None or not session_id:
+            return
+        try:
+            loop.call_soon_threadsafe(self._set_all, session_id)
+        except RuntimeError:
+            # Loop already closed (shutdown) — poll fallback covers stragglers.
+            pass
+
+    def _set_all(self, session_id: str) -> None:
+        for event in tuple(self._waiters.get(session_id, ())):
+            event.set()
+
+
+_SYNC_CURSOR_VERSION = "v1"
+
+
+def _encode_sync_cursor(session_cursor: Any, run_id: Optional[str], run_seq: Any) -> str:
+    """Encode the composite resume token carried as the SSE ``id:`` field.
+
+    Format: ``v1:<session_event_cursor>:<run_id>:<run_seq>``. run_ids never
+    contain colons; an empty run_id slot means "no active run observed yet".
+    """
+    return (
+        f"{_SYNC_CURSOR_VERSION}:{session_cursor or '0'}:{run_id or ''}:{int(run_seq or 0)}"
+    )
+
+
+def _parse_sync_cursor(token: Any) -> Optional[tuple[str, Optional[str], int]]:
+    """Parse a composite sync cursor; returns (session_cursor, run_id, run_seq).
+
+    Accepts a bare integer as a legacy session-event cursor (run state
+    defaults). Returns None for anything unparseable so callers can decide
+    between a 400 (explicit param) and a silent ignore (Last-Event-ID).
+    """
+    if token is None:
+        return None
+    text = str(token).strip()
+    if not text:
+        return None
+    if text.isdigit():
+        return text, None, 0
+    parts = text.split(":", 3)
+    if len(parts) != 4 or parts[0] != _SYNC_CURSOR_VERSION:
+        return None
+    session_cursor, run_id, run_seq_text = parts[1], parts[2], parts[3]
+    if not session_cursor.isdigit():
+        return None
+    try:
+        run_seq = int(run_seq_text or 0)
+    except ValueError:
+        return None
+    return session_cursor, run_id or None, run_seq
+
+
 def _redact_api_error_text(value: Any, *, limit: int | None = None) -> str:
     """Redact API-bound error text before it crosses the HTTP boundary."""
     redacted = redact_sensitive_text(str(value), force=True)
@@ -1092,6 +1222,12 @@ class APIServerAdapter(BasePlatformAdapter):
     # ``async_delivery_supported()``.
     supports_async_delivery: bool = False
 
+    # /subscribe poll-fallback interval (seconds). In-process writers wake
+    # subscribers instantly via SessionSyncNotifier; this bounds staleness for
+    # writes from OTHER processes sharing the same state.db. Class attribute
+    # so tests can shrink/stretch it per instance.
+    _SUBSCRIBE_POLL_INTERVAL: float = 2.0
+
     def __init__(self, config: PlatformConfig):
         super().__init__(config, Platform.API_SERVER)
         extra = config.extra or {}
@@ -1111,6 +1247,9 @@ class APIServerAdapter(BasePlatformAdapter):
         self._runner: Optional["web.AppRunner"] = None
         self._site: Optional["web.TCPSite"] = None
         self._response_store = ResponseStore()
+        # Push-wakeup fanout for /api/sessions/{id}/subscribe streams.
+        self._sync_notifier = SessionSyncNotifier()
+        self._session_event_listener_registered = False
         # Active run streams: run_id -> asyncio.Queue of SSE event dicts
         self._run_streams: Dict[str, "asyncio.Queue[Optional[Dict]]"] = {}
         # Creation timestamps for orphaned-run TTL sweep
@@ -1395,6 +1534,16 @@ class APIServerAdapter(BasePlatformAdapter):
                 self._session_db = SessionDB()
             except Exception as e:
                 logger.debug("SessionDB unavailable for API server: %s", e)
+        if self._session_db is not None and not self._session_event_listener_registered:
+            # Module-level hook: the agent constructs its own SessionDB over
+            # the same state.db, so instance-level hooks would miss the
+            # transcript writes that matter most to subscribers.
+            try:
+                from hermes_state import register_session_event_listener
+                register_session_event_listener(self._sync_notifier.notify)
+                self._session_event_listener_registered = True
+            except Exception as e:
+                logger.debug("session event listener registration failed: %s", e)
         return self._session_db
 
     # ------------------------------------------------------------------
@@ -1922,6 +2071,9 @@ class APIServerAdapter(BasePlatformAdapter):
                 "session_chat": True,
                 "session_chat_streaming": True,
                 "session_subscribe_sse": True,
+                # /subscribe frames carry a composite cursor as the SSE id;
+                # reconnects resume via Last-Event-ID or ?resume=.
+                "session_sync_resume": True,
                 "session_fork": True,
                 "session_archiving": True,
                 "unified_session_create": True,
@@ -2817,12 +2969,48 @@ class APIServerAdapter(BasePlatformAdapter):
 
         resolved_id = db.resolve_resume_session_id(session_id)
         resolved_session = db.get_session(resolved_id) or session
-        explicit_cursor = request.query.get("after") or request.query.get("cursor")
-        session_cursor = explicit_cursor or db.latest_session_event_cursor(resolved_id) or "0"
-        try:
-            run_after = int(request.query.get("run_after") or request.query.get("after_seq") or "0")
-        except ValueError:
-            return web.json_response(_openai_error("run_after must be an integer", code="invalid_run_after"), status=400)
+
+        # Resume-cursor precedence: Last-Event-ID header, then an explicit
+        # composite resume token, then the legacy split params. The header
+        # wins because EventSource auto-reconnects re-send the ORIGINAL query
+        # string — honoring a stale ?after= over the header would replay
+        # everything the client already consumed.
+        session_cursor: Optional[str] = None
+        resume_run_id: Optional[str] = None
+        run_after = 0
+
+        last_event_id = _parse_sync_cursor(request.headers.get("Last-Event-ID"))
+        if last_event_id is not None:
+            session_cursor, resume_run_id, run_after = last_event_id
+        else:
+            resume_param = request.query.get("resume")
+            if resume_param:
+                parsed = _parse_sync_cursor(resume_param)
+                if parsed is None:
+                    return web.json_response(
+                        _openai_error("resume must be a sync cursor token", code="invalid_resume_cursor"),
+                        status=400,
+                    )
+                session_cursor, resume_run_id, run_after = parsed
+            else:
+                explicit_cursor = request.query.get("after") or request.query.get("cursor")
+                if explicit_cursor:
+                    parsed = _parse_sync_cursor(explicit_cursor)
+                    if parsed is not None and parsed[1] is not None:
+                        session_cursor, resume_run_id, run_after = parsed
+                    else:
+                        session_cursor = explicit_cursor
+                raw_run_after = request.query.get("run_after") or request.query.get("after_seq")
+                if raw_run_after is not None:
+                    try:
+                        run_after = int(raw_run_after)
+                    except ValueError:
+                        return web.json_response(
+                            _openai_error("run_after must be an integer", code="invalid_run_after"),
+                            status=400,
+                        )
+        if not session_cursor:
+            session_cursor = db.latest_session_event_cursor(resolved_id) or "0"
         once = _coerce_request_bool(request.query.get("once"), default=False)
 
         headers = {
@@ -2838,11 +3026,27 @@ class APIServerAdapter(BasePlatformAdapter):
         response = web.StreamResponse(status=200, headers=headers)
         await response.prepare(request)
 
+        latest_run = self._response_store.get_latest_session_run(resolved_id)
+        active_run_id = latest_run.get("run_id") if latest_run else None
+        active_run_status = latest_run.get("status") if latest_run else None
+        if resume_run_id is not None and resume_run_id != active_run_id:
+            # The client's run cursor belongs to a finished run; run.switch
+            # below tells it to reset, and the new run replays from 0.
+            active_run_seq = 0
+        else:
+            active_run_seq = run_after
+
         async def _write_event(event_name: str, payload: Dict[str, Any]) -> None:
+            # Every frame carries the composite resume token as its SSE id so
+            # EventSource reconnects (Last-Event-ID) resume without client
+            # bookkeeping. Cursors are advanced BEFORE the write, so the id
+            # always means "everything up to and including this frame".
+            token = _encode_sync_cursor(session_cursor, active_run_id, active_run_seq)
             data = json.dumps(payload, ensure_ascii=False)
-            await response.write(f"event: {event_name}\ndata: {data}\n\n".encode("utf-8"))
+            await response.write(
+                f"id: {token}\nevent: {event_name}\ndata: {data}\n\n".encode("utf-8")
+            )
 
-        latest_run = self._response_store.get_latest_session_run(resolved_id)
         snapshot = {
             "object": "hermes.session.snapshot",
             "session_id": resolved_id,
@@ -2851,25 +3055,31 @@ class APIServerAdapter(BasePlatformAdapter):
             "items": db.get_session_items(resolved_id),
             "latest_event_cursor": db.latest_session_event_cursor(resolved_id),
             "active_run": latest_run,
+            "cursor": _encode_sync_cursor(session_cursor, active_run_id, active_run_seq),
         }
-        active_run_id = latest_run.get("run_id") if latest_run else None
-        active_run_seq = run_after
         last_activity = time.monotonic()
 
+        waiter = self._sync_notifier.attach(resolved_id)
         try:
             await _write_event("session.snapshot", snapshot)
             while True:
+                # Clear before reading: a write landing during the reads
+                # re-sets the flag and the next wait returns immediately, so
+                # no event can slip between "drained stores" and "went to sleep".
+                waiter.clear()
                 wrote = False
                 for event in db.list_session_events(session_id=resolved_id, after=session_cursor, limit=100):
-                    await _write_event(str(event.get("event_type") or "session.event"), event)
                     session_cursor = event.get("cursor") or session_cursor
+                    await _write_event(str(event.get("event_type") or "session.event"), event)
                     wrote = True
 
                 latest_run = self._response_store.get_latest_session_run(resolved_id)
                 latest_run_id = latest_run.get("run_id") if latest_run else None
+                latest_run_status = latest_run.get("status") if latest_run else None
                 if latest_run_id and latest_run_id != active_run_id:
                     active_run_id = latest_run_id
                     active_run_seq = 0
+                    active_run_status = latest_run_status
                     await _write_event("run.switch", {"session_id": resolved_id, "run": latest_run})
                     wrote = True
                 if active_run_id:
@@ -2878,6 +3088,13 @@ class APIServerAdapter(BasePlatformAdapter):
                         active_run_seq = max(active_run_seq, int(payload.get("seq") or active_run_seq))
                         await _write_event(str(item.get("event") or "run.event"), payload)
                         wrote = True
+                    if latest_run_id == active_run_id and latest_run_status != active_run_status:
+                        # Status changed without a replayable event (e.g.
+                        # /halt) — surface the transition so clients don't
+                        # keep rendering a dead run as live.
+                        active_run_status = latest_run_status
+                        await _write_event("run.status", {"session_id": resolved_id, "run": latest_run})
+                        wrote = True
 
                 if once:
                     break
@@ -2886,11 +3103,20 @@ class APIServerAdapter(BasePlatformAdapter):
                 elif time.monotonic() - last_activity >= CHAT_COMPLETIONS_SSE_KEEPALIVE_SECONDS:
                     await response.write(b": keepalive\n\n")
                     last_activity = time.monotonic()
-                await asyncio.sleep(0.5)
+                try:
+                    # Push-first: writers signal the notifier on every durable
+                    # append. The timeout is only a fallback for writers in
+                    # OTHER processes (CLI/gateway sharing state.db), which
+                    # the in-process notifier cannot see.
+                    await asyncio.wait_for(waiter.wait(), timeout=self._SUBSCRIBE_POLL_INTERVAL)
+                except asyncio.TimeoutError:
+                    pass
         except (asyncio.CancelledError, ConnectionResetError):
             raise
         except Exception as exc:
             logger.debug("[api_server] session subscribe stream error for %s: %s", resolved_id, exc)
+        finally:
+            self._sync_notifier.detach(resolved_id, waiter)
         return response
 
     async def _handle_session_chat(self, request: "web.Request") -> "web.Response":
@@ -3017,6 +3243,7 @@ class APIServerAdapter(BasePlatformAdapter):
         run_id = f"run_{uuid.uuid4().hex}"
         agent_ref = [None]
         self._response_store.create_session_run(run_id, session_id, user_message)
+        self._sync_notifier.notify(session_id)
 
         def _event_payload(name: str, payload: Dict[str, Any]) -> tuple[str, Dict[str, Any]]:
             payload.setdefault("session_id", session_id)
@@ -3024,6 +3251,7 @@ class APIServerAdapter(BasePlatformAdapter):
             payload.setdefault("ts", time.time())
             try:
                 payload = self._response_store.put_session_run_event(run_id, name, payload)
+                self._sync_notifier.notify(session_id)
             except Exception:
                 logger.debug("[api_server] failed to persist session run event", exc_info=True)
             return name, payload
@@ -3135,6 +3363,7 @@ class APIServerAdapter(BasePlatformAdapter):
                     run_id = str(queued.get("run_id") or f"run_{uuid.uuid4().hex}")
                     message_id = f"msg_{uuid.uuid4().hex}"
                     self._response_store.create_session_run(run_id, session_id, queued.get("user_message"))
+                    self._sync_notifier.notify(session_id)
                     await _run_turn(
                         queued.get("user_message"),
                         queued.get("system_prompt"),
@@ -6448,6 +6677,7 @@ class APIServerAdapter(BasePlatformAdapter):
             return False
 
         try:
+            self._sync_notifier.bind_loop(asyncio.get_running_loop())
             mws = [mw for mw in (cors_middleware, body_limit_middleware, security_headers_middleware) if mw is not None]
             self._app = web.Application(middlewares=mws, client_max_size=MAX_REQUEST_BYTES)
             assert self._app is not None
diff --git a/hermes_state.py b/hermes_state.py
index 3d83c0f94..13445c660 100644
--- a/hermes_state.py
+++ b/hermes_state.py
@@ -169,6 +169,37 @@ _last_init_error_lock = threading.Lock()
 _wal_fallback_warned_paths: set[str] = set()
 _wal_fallback_warned_lock = threading.Lock()
 
+# In-process wakeup hooks fired after a write commits new session_events rows.
+# Module-level (not per-SessionDB) because several SessionDB instances can
+# share one state.db within a process (the API server and the agent each
+# construct their own); a subscriber must wake regardless of which instance
+# performed the write. Listeners take the session_id and must be non-blocking;
+# out-of-process writers are invisible here, so consumers keep a poll fallback.
+_session_event_listeners: "set[Callable[[str], None]]" = set()
+_session_event_listeners_lock = threading.Lock()
+
+
+def register_session_event_listener(listener: Callable[[str], None]) -> None:
+    with _session_event_listeners_lock:
+        _session_event_listeners.add(listener)
+
+
+def unregister_session_event_listener(listener: Callable[[str], None]) -> None:
+    with _session_event_listeners_lock:
+        _session_event_listeners.discard(listener)
+
+
+def _notify_session_event_listeners(session_id: str) -> None:
+    with _session_event_listeners_lock:
+        listeners = tuple(_session_event_listeners)
+    for listener in listeners:
+        try:
+            listener(session_id)
+        except Exception:
+            logger.debug(
+                "session event listener failed for %s", session_id, exc_info=True
+            )
+
 _FTS_TRIGGERS = (
     "messages_fts_insert",
     "messages_fts_delete",
@@ -1443,6 +1474,7 @@ class SessionDB:
             )
 
         event_row_id = self._execute_write(_do)
+        _notify_session_event_listeners(session_id)
         events = self.list_session_events(after=event_row_id - 1, limit=1)
         return events[0]
 
@@ -2077,6 +2109,7 @@ class SessionDB:
                     created_at=ended_at,
                 )
         self._execute_write(_do)
+        _notify_session_event_listeners(session_id)
 
     def reopen_session(self, session_id: str) -> None:
         """Clear ended_at/end_reason so a session can be resumed."""
@@ -2092,6 +2125,7 @@ class SessionDB:
                 payload={"session_id": session_id, "state": "idle"},
             )
         self._execute_write(_do)
+        _notify_session_event_listeners(session_id)
 
     def update_session_cwd(
         self, session_id: str, cwd: str, git_branch: str = None, git_repo_root: str = None
@@ -3564,7 +3598,9 @@ class SessionDB:
                 )
             return msg_id
 
-        return self._execute_write(_do)
+        msg_row_id = self._execute_write(_do)
+        _notify_session_event_listeners(session_id)
+        return msg_row_id
 
     def _insert_message_rows(self, conn, session_id: str, messages: List[Dict[str, Any]]) -> tuple[int, int]:
         """Insert *messages* as fresh active rows for *session_id*.
@@ -3728,6 +3764,7 @@ class SessionDB:
             )
 
         self._execute_write(_do)
+        _notify_session_event_listeners(session_id)
 
     def has_archived_messages(self, session_id: str) -> bool:
         """Return True if the session has any soft-archived (``active = 0``) rows.
@@ -3793,7 +3830,9 @@ class SessionDB:
             )
             return inserted
 
-        return self._execute_write(_do)
+        inserted_count = self._execute_write(_do)
+        _notify_session_event_listeners(session_id)
+        return inserted_count
 
 
     def get_messages(
diff --git a/tests/gateway/test_session_api.py b/tests/gateway/test_session_api.py
index 203204654..8ecac3a1b 100644
--- a/tests/gateway/test_session_api.py
+++ b/tests/gateway/test_session_api.py
@@ -1154,6 +1154,10 @@ async def test_session_subscribe_streams_events_written_after_connect(adapter, s
             "run.completed",
             {"message_id": "msg_live", "completed": True},
         )
+        # Real writers signal the notifier after persisting (see
+        # _event_payload); emulate that so the subscriber wakes via push
+        # instead of waiting out the poll-fallback interval.
+        adapter._sync_notifier.notify(session_id)
 
         seen_delta = None
         for _ in range(160):
@@ -1183,7 +1187,14 @@ def test_response_store_marks_incomplete_session_runs_interrupted_after_restart(
         assert run["status"] == "interrupted"
         assert "restarted" in run["error"]
         events = restarted_store.list_session_run_events("run_pending")
-        assert [event["event"] for event in events] == ["run.started"]
+        assert [event["event"] for event in events] == ["run.started", "run.interrupted"]
+        interrupted_payload = events[-1]["payload"]
+        assert interrupted_payload["status"] == "interrupted"
+        assert "restarted" in interrupted_payload["message"]
+        # The synthesized terminal event must be replayable from the client's
+        # pre-restart cursor position.
+        resumed = restarted_store.list_session_run_events("run_pending", after=1)
+        assert [event["event"] for event in resumed] == ["run.interrupted"]
     finally:
         restarted_store.close()
 
diff --git a/tests/gateway/test_session_sync.py b/tests/gateway/test_session_sync.py
new file mode 100644
index 000000000..f50a88317
--- /dev/null
+++ b/tests/gateway/test_session_sync.py
@@ -0,0 +1,464 @@
+"""Behavioral tests for the durable, realtime, resumable session sync engine.
+
+Covers the /api/sessions/{id}/subscribe protocol: composite resume cursors
+carried as SSE ids, Last-Event-ID reconnects, legacy cursor params,
+multi-client fanout, push (notifier) wakeups beating the poll fallback, and
+server-restart interruption visibility. See docs/api-session-sync.md.
+"""
+
+import asyncio
+import json
+import threading
+
+import pytest
+from aiohttp import web
+from aiohttp.test_utils import TestClient, TestServer
+
+import hermes_state
+from gateway.config import PlatformConfig
+from gateway.platforms.api_server import (
+    APIServerAdapter,
+    ResponseStore,
+    _encode_sync_cursor,
+    _parse_sync_cursor,
+)
+from hermes_state import SessionDB
+
+
+@pytest.fixture
+def session_db(tmp_path):
+    db = SessionDB(tmp_path / "state.db")
+    try:
+        yield db
+    finally:
+        db.close()
+
+
+@pytest.fixture
+def adapter(session_db, tmp_path):
+    adapter = APIServerAdapter(PlatformConfig(enabled=True))
+    adapter._response_store.close()
+    adapter._response_store = ResponseStore(db_path=str(tmp_path / "response_store.db"))
+    adapter._session_db = session_db
+    try:
+        yield adapter
+    finally:
+        adapter._response_store.close()
+        # _ensure_session_db registers the adapter's notifier in the
+        # module-level hermes_state listener registry; drop it so adapters
+        # never leak across tests.
+        hermes_state.unregister_session_event_listener(adapter._sync_notifier.notify)
+
+
+def _create_app(adapter: APIServerAdapter) -> web.Application:
+    app = web.Application()
+    app.router.add_get(
+        "/api/sessions/{session_id}/subscribe", adapter._handle_session_subscribe
+    )
+    return app
+
+
+def _parse_sse(raw: str) -> list:
+    """Parse SSE text into a list of {id, event, data} frames."""
+    frames = []
+    for block in raw.split("\n\n"):
+        frame = {"id": None, "event": None, "data": None}
+        for line in block.splitlines():
+            if line.startswith("id: "):
+                frame["id"] = line[len("id: "):]
+            elif line.startswith("event: "):
+                frame["event"] = line[len("event: "):]
+            elif line.startswith("data: "):
+                frame["data"] = json.loads(line[len("data: "):])
+        if frame["event"] is not None:
+            frames.append(frame)
+    return frames
+
+
+async def _read_frames_until(resp, predicate, timeout=5.0):
+    """Incrementally read SSE frames from a live stream until predicate hits."""
+    frames = []
+    buffer = b""
+    loop = asyncio.get_running_loop()
+    deadline = loop.time() + timeout
+    while True:
+        remaining = deadline - loop.time()
+        if remaining <= 0:
+            raise AssertionError(f"predicate not satisfied; frames so far: {frames}")
+        chunk = await asyncio.wait_for(resp.content.read(4096), timeout=remaining)
+        if not chunk:
+            raise AssertionError(f"stream closed early; frames so far: {frames}")
+        buffer += chunk
+        while b"\n\n" in buffer:
+            block, buffer = buffer.split(b"\n\n", 1)
+            parsed = _parse_sse(block.decode("utf-8") + "\n\n")
+            frames.extend(parsed)
+            for frame in parsed:
+                if predicate(frame):
+                    return frames
+
+
+def _seed_session_with_run(session_db, response_store, name="sync-session"):
+    session_id = session_db.create_session(name, "api_server")
+    session_db.append_message(session_id, "user", "hello")
+    session_db.append_message(session_id, "assistant", "hi there")
+    run_id = "run_sync_test"
+    response_store.create_session_run(run_id, session_id, "hello")
+    response_store.put_session_run_event(run_id, "run.started", {"status": "running"})
+    response_store.put_session_run_event(
+        run_id, "assistant.delta", {"message_id": "msg_1", "delta": "hi "}
+    )
+    response_store.put_session_run_event(
+        run_id, "assistant.delta", {"message_id": "msg_1", "delta": "there"}
+    )
+    return session_id, run_id
+
+
+def test_sync_cursor_roundtrip_and_legacy_forms():
+    token = _encode_sync_cursor("42", "run_abc", 7)
+    assert _parse_sync_cursor(token) == ("42", "run_abc", 7)
+    # No active run: empty run slot decodes to None.
+    assert _parse_sync_cursor(_encode_sync_cursor("9", None, 0)) == ("9", None, 0)
+    # Bare integers are legacy session-event cursors.
+    assert _parse_sync_cursor("123") == ("123", None, 0)
+    # Garbage is rejected, not misparsed.
+    assert _parse_sync_cursor("v2:1:x:0") is None
+    assert _parse_sync_cursor("v1:not-a-number:run:0") is None
+    assert _parse_sync_cursor("") is None
+    assert _parse_sync_cursor(None) is None
+
+
+@pytest.mark.asyncio
+async def test_subscribe_frames_carry_composite_cursor_id(adapter, session_db):
+    session_id, run_id = _seed_session_with_run(session_db, adapter._response_store)
+
+    app = _create_app(adapter)
+    async with TestClient(TestServer(app)) as cli:
+        resp = await cli.get(f"/api/sessions/{session_id}/subscribe?after=0&once=true")
+        assert resp.status == 200
+        frames = _parse_sse(await resp.text())
+
+    assert frames, "expected at least the snapshot frame"
+    assert frames[0]["event"] == "session.snapshot"
+    for frame in frames:
+        parsed = _parse_sync_cursor(frame["id"])
+        assert parsed is not None, f"frame without a valid id: {frame}"
+    # The final frame's id reflects everything delivered: the latest durable
+    # cursor and the last replayed run seq.
+    final_cursor, final_run_id, final_seq = _parse_sync_cursor(frames[-1]["id"])
+    assert final_cursor == session_db.latest_session_event_cursor(session_id)
+    assert final_run_id == run_id
+    assert final_seq == adapter._response_store.get_session_run(run_id)["last_seq"]
+    # The snapshot payload also exposes the token for non-EventSource clients.
+    assert _parse_sync_cursor(frames[0]["data"]["cursor"]) is not None
+
+
+@pytest.mark.asyncio
+async def test_resume_from_composite_cursor_skips_replayed_events(adapter, session_db):
+    session_id, run_id = _seed_session_with_run(session_db, adapter._response_store)
+
+    app = _create_app(adapter)
+    async with TestClient(TestServer(app)) as cli:
+        resp = await cli.get(f"/api/sessions/{session_id}/subscribe?after=0&once=true")
+        first_frames = _parse_sse(await resp.text())
+        resume_token = first_frames[-1]["id"]
+        first_deltas = [
+            f["data"]["delta"]
+            for f in first_frames
+            if f["event"] == "assistant.delta"
+        ]
+        assert first_deltas == ["hi ", "there"]
+
+        # New activity lands after the client disconnected.
+        session_db.append_message(session_id, "user", "follow-up question")
+        adapter._response_store.put_session_run_event(
+            run_id, "assistant.delta", {"message_id": "msg_1", "delta": "!"}
+        )
+
+        resp = await cli.get(
+            f"/api/sessions/{session_id}/subscribe?resume={resume_token}&once=true"
+        )
+        assert resp.status == 200
+        resumed_frames = _parse_sse(await resp.text())
+
+    resumed_deltas = [
+        f["data"]["delta"] for f in resumed_frames if f["event"] == "assistant.delta"
+    ]
+    assert resumed_deltas == ["!"], "already-consumed deltas must not be replayed"
+    resumed_items = [
+        f for f in resumed_frames if f["event"] == "session.item.upserted"
+    ]
+    contents = json.dumps([f["data"] for f in resumed_items])
+    assert "follow-up question" in contents
+    assert "hi there" not in contents, "pre-cursor durable events must not be replayed"
+
+
+@pytest.mark.asyncio
+async def test_last_event_id_header_beats_stale_query_params(adapter, session_db):
+    session_id, run_id = _seed_session_with_run(session_db, adapter._response_store)
+
+    app = _create_app(adapter)
+    async with TestClient(TestServer(app)) as cli:
+        resp = await cli.get(f"/api/sessions/{session_id}/subscribe?after=0&once=true")
+        resume_token = _parse_sse(await resp.text())[-1]["id"]
+
+        adapter._response_store.put_session_run_event(
+            run_id, "assistant.delta", {"message_id": "msg_1", "delta": "new"}
+        )
+
+        # An EventSource reconnect re-sends the ORIGINAL query string but adds
+        # Last-Event-ID; the header must win or the client replays everything.
+        resp = await cli.get(
+            f"/api/sessions/{session_id}/subscribe?after=0&once=true",
+            headers={"Last-Event-ID": resume_token},
+        )
+        frames = _parse_sse(await resp.text())
+
+    deltas = [f["data"]["delta"] for f in frames if f["event"] == "assistant.delta"]
+    assert deltas == ["new"]
+
+
+@pytest.mark.asyncio
+async def test_legacy_after_and_run_after_params_still_work(adapter, session_db):
+    session_id, run_id = _seed_session_with_run(session_db, adapter._response_store)
+    latest_cursor = session_db.latest_session_event_cursor(session_id)
+
+    app = _create_app(adapter)
+    async with TestClient(TestServer(app)) as cli:
+        # Bare-integer after + run_after replay only what's past each cursor.
+        resp = await cli.get(
+            f"/api/sessions/{session_id}/subscribe?after={latest_cursor}&run_after=2&once=true"
+        )
+        frames = _parse_sse(await resp.text())
+        deltas = [f["data"]["delta"] for f in frames if f["event"] == "assistant.delta"]
+        assert deltas == ["there"], "run_after=2 must skip seqs 1-2"
+        assert not [f for f in frames if f["event"] == "session.item.upserted"]
+
+        # after_seq is an accepted alias, and cursor an alias for after.
+        resp = await cli.get(
+            f"/api/sessions/{session_id}/subscribe?cursor={latest_cursor}&after_seq=3&once=true"
+        )
+        frames = _parse_sse(await resp.text())
+        assert not [f for f in frames if f["event"] == "assistant.delta"]
+
+        # A malformed run_after is a 400, not a silent default.
+        resp = await cli.get(
+            f"/api/sessions/{session_id}/subscribe?run_after=nope&once=true"
+        )
+        assert resp.status == 400
+        # As is a malformed resume token.
+        resp = await cli.get(
+            f"/api/sessions/{session_id}/subscribe?resume=v9:bogus&once=true"
+        )
+        assert resp.status == 400
+
+
+@pytest.mark.asyncio
+async def test_multi_client_concurrent_subscribe_both_receive_events(adapter, session_db):
+    session_id, run_id = _seed_session_with_run(session_db, adapter._response_store)
+
+    app = _create_app(adapter)
+    async with TestClient(TestServer(app)) as cli:
+        resp_a = await cli.get(f"/api/sessions/{session_id}/subscribe")
+        resp_b = await cli.get(f"/api/sessions/{session_id}/subscribe")
+        assert resp_a.status == 200 and resp_b.status == 200
+
+        def is_live_delta(frame):
+            return (
+                frame["event"] == "assistant.delta"
+                and frame["data"].get("delta") == "broadcast"
+            )
+
+        # Give both streams their snapshots before writing.
+        await _read_frames_until(resp_a, lambda f: f["event"] == "session.snapshot")
+        await _read_frames_until(resp_b, lambda f: f["event"] == "session.snapshot")
+
+        adapter._response_store.put_session_run_event(
+            run_id, "assistant.delta", {"message_id": "msg_1", "delta": "broadcast"}
+        )
+        adapter._sync_notifier.notify(session_id)
+
+        frames_a, frames_b = await asyncio.gather(
+            _read_frames_until(resp_a, is_live_delta),
+            _read_frames_until(resp_b, is_live_delta),
+        )
+
+    assert any(is_live_delta(f) for f in frames_a)
+    assert any(is_live_delta(f) for f in frames_b)
+
+
+@pytest.mark.asyncio
+async def test_push_wakeup_beats_poll_interval(adapter, session_db):
+    """Delivery must ride the notifier, not the poll timer."""
+    session_id, run_id = _seed_session_with_run(session_db, adapter._response_store)
+    adapter._SUBSCRIBE_POLL_INTERVAL = 30.0
+
+    app = _create_app(adapter)
+    async with TestClient(TestServer(app)) as cli:
+        resp = await cli.get(f"/api/sessions/{session_id}/subscribe")
+        await _read_frames_until(resp, lambda f: f["event"] == "session.snapshot")
+
+        adapter._response_store.put_session_run_event(
+            run_id, "assistant.delta", {"message_id": "msg_1", "delta": "instant"}
+        )
+        adapter._sync_notifier.notify(session_id)
+
+        frames = await _read_frames_until(
+            resp,
+            lambda f: f["event"] == "assistant.delta"
+            and f["data"].get("delta") == "instant",
+            timeout=5.0,
+        )
+    assert frames
+
+
+@pytest.mark.asyncio
+async def test_append_message_from_thread_wakes_subscriber(adapter, session_db):
+    """Real path: the agent persists a message on an executor thread through
+    the module-level hermes_state listener hook, and a connected subscriber
+    receives the durable item without waiting out the poll interval."""
+    session_id = session_db.create_session("thread-wake-session", "api_server")
+    adapter._SUBSCRIBE_POLL_INTERVAL = 30.0
+    # Registration normally happens in _ensure_session_db on first handler use;
+    # the subscribe request below triggers it.
+
+    app = _create_app(adapter)
+    async with TestClient(TestServer(app)) as cli:
+        resp = await cli.get(f"/api/sessions/{session_id}/subscribe")
+        await _read_frames_until(resp, lambda f: f["event"] == "session.snapshot")
+
+        def _write_from_thread():
+            # A second SessionDB instance over the same state.db — the agent
+            # never shares the adapter's instance.
+            agent_db = SessionDB(session_db.db_path)
+            try:
+                agent_db.append_message(session_id, "assistant", "written off-loop")
+            finally:
+                agent_db.close()
+
+        thread = threading.Thread(target=_write_from_thread)
+        thread.start()
+        try:
+            frames = await _read_frames_until(
+                resp,
+                lambda f: f["event"] == "session.item.upserted"
+                and "written off-loop" in json.dumps(f["data"]),
+                timeout=10.0,
+            )
+        finally:
+            thread.join(timeout=5)
+    assert frames
+
+
+@pytest.mark.asyncio
+async def test_restart_interruption_visible_to_resuming_and_fresh_clients(
+    adapter, session_db, tmp_path
+):
+    session_id, run_id = _seed_session_with_run(session_db, adapter._response_store)
+
+    app = _create_app(adapter)
+    async with TestClient(TestServer(app)) as cli:
+        resp = await cli.get(f"/api/sessions/{session_id}/subscribe?after=0&once=true")
+        resume_token = _parse_sse(await resp.text())[-1]["id"]
+
+        # Simulate a server restart: a new ResponseStore over the same file
+        # runs startup recovery in __init__.
+        adapter._response_store.close()
+        adapter._response_store = ResponseStore(
+            db_path=str(tmp_path / "response_store.db")
+        )
+
+        # A client resuming from its pre-restart cursor replays the synthetic
+        # terminal event instead of hanging on a run that will never finish.
+        resp = await cli.get(
+            f"/api/sessions/{session_id}/subscribe?resume={resume_token}&once=true"
+        )
+        resumed_frames = _parse_sse(await resp.text())
+        interrupted = [f for f in resumed_frames if f["event"] == "run.interrupted"]
+        assert len(interrupted) == 1
+        assert interrupted[0]["data"]["run_id"] == run_id
+        assert interrupted[0]["data"]["status"] == "interrupted"
+
+        # A fresh client sees the truth in the snapshot.
+        resp = await cli.get(f"/api/sessions/{session_id}/subscribe?once=true")
+        snapshot = _parse_sse(await resp.text())[0]
+        assert snapshot["data"]["active_run"]["status"] == "interrupted"
+
+
+@pytest.mark.asyncio
+async def test_join_in_progress_replays_run_and_switches_to_new_run(adapter, session_db):
+    session_id, run_id = _seed_session_with_run(session_db, adapter._response_store)
+
+    app = _create_app(adapter)
+    async with TestClient(TestServer(app)) as cli:
+        # A client joining mid-run gets the snapshot plus the full in-flight
+        # run replay by default (run_after defaults to 0).
+        resp = await cli.get(f"/api/sessions/{session_id}/subscribe")
+        frames = await _read_frames_until(
+            resp,
+            lambda f: f["event"] == "assistant.delta"
+            and f["data"].get("delta") == "there",
+        )
+        assert frames[0]["event"] == "session.snapshot"
+        assert frames[0]["data"]["active_run"]["run_id"] == run_id
+        deltas = [f["data"]["delta"] for f in frames if f["event"] == "assistant.delta"]
+        assert deltas == ["hi ", "there"]
+
+        # When a new run starts, live subscribers get run.switch and then the
+        # new run's events from seq 0.
+        adapter._response_store.create_session_run(
+            "run_sync_next", session_id, "next turn"
+        )
+        adapter._response_store.put_session_run_event(
+            "run_sync_next", "run.started", {"status": "running"}
+        )
+        adapter._sync_notifier.notify(session_id)
+
+        frames = await _read_frames_until(
+            resp,
+            lambda f: f["event"] == "run.started"
+            and f["data"].get("run_id") == "run_sync_next",
+        )
+        switch = [f for f in frames if f["event"] == "run.switch"]
+        assert len(switch) == 1
+        assert switch[0]["data"]["run"]["run_id"] == "run_sync_next"
+
+
+@pytest.mark.asyncio
+async def test_resume_token_for_finished_run_replays_new_run_from_start(
+    adapter, session_db
+):
+    """A composite cursor whose run_id no longer matches the session's latest
+    run must not suppress the new run's events."""
+    session_id, run_id = _seed_session_with_run(session_db, adapter._response_store)
+
+    app = _create_app(adapter)
+    async with TestClient(TestServer(app)) as cli:
+        resp = await cli.get(f"/api/sessions/{session_id}/subscribe?once=true")
+        resume_token = _parse_sse(await resp.text())[-1]["id"]
+
+        adapter._response_store.put_session_run_event(
+            run_id, "run.completed", {"completed": True}
+        )
+        adapter._response_store.create_session_run(
+            "run_sync_second", session_id, "again"
+        )
+        adapter._response_store.put_session_run_event(
+            "run_sync_second", "run.started", {"status": "running"}
+        )
+        adapter._response_store.put_session_run_event(
+            "run_sync_second",
+            "assistant.delta",
+            {"message_id": "msg_2", "delta": "second run"},
+        )
+
+        resp = await cli.get(
+            f"/api/sessions/{session_id}/subscribe?resume={resume_token}&once=true"
+        )
+        frames = _parse_sse(await resp.text())
+
+    deltas = [f["data"]["delta"] for f in frames if f["event"] == "assistant.delta"]
+    assert "second run" in deltas, (
+        "old-run seq must not mask the new run's replay: got "
+        f"{[f['event'] for f in frames]}"
+    )
diff --git a/tests/test_hermes_state.py b/tests/test_hermes_state.py
index dac1eb0ce..a14e534e0 100644
--- a/tests/test_hermes_state.py
+++ b/tests/test_hermes_state.py
@@ -4938,3 +4938,77 @@ def test_refresh_compression_lock_requires_holder_and_preserves_reclaimability(d
 
     monkeypatch.setattr(hermes_state.time, "time", lambda: 1016.0)
     assert db.try_acquire_compression_lock("s1", "holder-b", ttl_seconds=10.0) is True
+
+
+class TestSessionEventListeners:
+    """Module-level wakeup hooks fired after session_events writes commit."""
+
+    def test_listener_fires_for_event_writers(self, db):
+        db.create_session("s1", "cli")
+        seen = []
+        hermes_state.register_session_event_listener(seen.append)
+        try:
+            db.append_message("s1", "user", "hello")
+            assert seen == ["s1"]
+
+            db.append_session_event("s1", "session.custom", {"k": "v"})
+            assert seen == ["s1", "s1"]
+
+            db.end_session("s1", "done")
+            db.reopen_session("s1")
+            assert seen == ["s1"] * 4
+
+            db.replace_messages("s1", [{"role": "user", "content": "rewritten"}])
+            assert seen == ["s1"] * 5
+
+            db.archive_and_compact("s1", [{"role": "user", "content": "compacted"}])
+            assert seen == ["s1"] * 6
+        finally:
+            hermes_state.unregister_session_event_listener(seen.append)
+
+    def test_raising_listener_does_not_break_writes(self, db):
+        db.create_session("s1", "cli")
+
+        def _explode(session_id):
+            raise RuntimeError("listener bug")
+
+        hermes_state.register_session_event_listener(_explode)
+        try:
+            msg_id = db.append_message("s1", "user", "still persisted")
+            assert msg_id > 0
+            assert db.get_messages("s1")[-1]["content"] == "still persisted"
+        finally:
+            hermes_state.unregister_session_event_listener(_explode)
+
+    def test_unregistered_listener_stops_firing(self, db):
+        db.create_session("s1", "cli")
+        seen = []
+
+        def _listener(session_id):
+            seen.append(session_id)
+
+        hermes_state.register_session_event_listener(_listener)
+        db.append_message("s1", "user", "one")
+        hermes_state.unregister_session_event_listener(_listener)
+        db.append_message("s1", "user", "two")
+        assert seen == ["s1"]
+
+    def test_listener_fires_across_instances_sharing_a_db(self, tmp_path):
+        """The registry is module-level: a write through ANY SessionDB
+        instance (the agent constructs its own) must wake listeners."""
+        db_a = hermes_state.SessionDB(db_path=tmp_path / "shared_state.db")
+        db_b = hermes_state.SessionDB(db_path=tmp_path / "shared_state.db")
+        seen = []
+
+        def _listener(session_id):
+            seen.append(session_id)
+
+        hermes_state.register_session_event_listener(_listener)
+        try:
+            db_a.create_session("s1", "cli")
+            db_b.append_message("s1", "user", "written via second instance")
+            assert seen == ["s1"]
+        finally:
+            hermes_state.unregister_session_event_listener(_listener)
+            db_a.close()
+            db_b.close()
-- 
2.52.0

